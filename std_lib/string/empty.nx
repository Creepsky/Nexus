/**
  * @brief Returns, if the string has the size of 0.
  * @returns true, if the string is empty, false if not.
  */
bool string.empty()
{
    return new bool(c++ {|
        __this.value.empty()
    |});
}